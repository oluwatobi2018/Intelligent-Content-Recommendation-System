# Database Configuration
DB_USER=remmyadeogun
DB_HOST=localhost
DB_NAME=content_recommendation
DB_PASSWORD=your_secure_password_here
DB_PORT=5432

# Full Database URL (for ORMs like Sequelize/Prisma)
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Environment Mode
NODE_ENV=development  # Change to 'production' in live server

# Debugging (Enable detailed logs in development)
DEBUG=true
