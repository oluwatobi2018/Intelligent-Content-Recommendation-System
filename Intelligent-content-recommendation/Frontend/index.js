/src
  /components   // React components
  /store        // Redux store setup
  /assets       // Images, styles, etc.
  App.js        // Main App component
  index.js      // Entry point for the application
  store.js      // Redux store setup (if it isn’t already separate)
